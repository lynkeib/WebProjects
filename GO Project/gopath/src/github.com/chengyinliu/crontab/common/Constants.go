package common

const(
	// job dir
	JOB_SAVE_DIR = "/cron/jobs/"
	// jon kill dir
	JOB_KILL_DIR = "/cron/killer/"
	// save event
	JOB_EVENT_SAVE = 1
	// delete event
	JOB_EVENT_DELETE = 2
	// kill event
	JOB_EVENT_KILL = 3
	// lock dir
	JOB_LOCK_DIR = "/cron/lock/"
)
