// $ for selection
// $("h1:first-of-type")， select the first element in this type, nth-of-type
// .text(), get all the text in the label, or change the text
// .html(), get the html contents in the selection
// .css(), change css for the selection
// .attr(), change or add attributes, can do multiple adding
// .val(), wrapper for .value using for input tag and select tag, works on text, color, checkbox
// .addClass(), .removeClass(), .toggleCLass()

// Events
// .click(), add click listern
// ,Keypress(), will be tragged if the user press a key, or press a certain key (input for the function should be "event")
// .on(), can replace all the events methods, .on("click", function(){})
// this in jQuery is $(this)